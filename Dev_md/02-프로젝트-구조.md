# 프로젝트 구조

## 디렉토리 트리

```
D:\dreamit-web\Research\
├── .github/
│   └── workflows/
│       └── deploy.yml              # GitHub Pages 자동 배포
├── Dev_md/                          # 개발 문서
├── public/
│   ├── CNAME                        # research.dreamitbiz.com
│   ├── robots.txt                   # 검색엔진 크롤링 설정
│   └── sitemap.xml                  # 사이트맵
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Navbar.tsx           # 내비게이션 바 (8개 학습경로 메뉴)
│   │   │   └── Footer.tsx           # 푸터
│   │   ├── AuthGuard.tsx            # 인증 보호 래퍼
│   │   ├── CodeBlock.tsx            # 코드 블록 컴포넌트
│   │   ├── CommentSection.tsx       # 댓글 섹션
│   │   ├── FeatureCard.tsx          # 기능 카드
│   │   ├── HeroBackground.tsx       # 히어로 배경 애니메이션
│   │   ├── HeroCarousel.tsx         # 히어로 캐러셀
│   │   ├── ImageUpload.tsx          # 이미지 업로드
│   │   ├── Pagination.tsx           # 페이지네이션
│   │   ├── SearchModal.tsx          # 검색 모달
│   │   ├── SEOHead.tsx              # SEO 메타태그
│   │   └── TipBox.tsx               # 팁/경고/중요 박스
│   ├── config/
│   │   ├── site.ts                  # 사이트 설정 + 8개 LEARNING_PATHS
│   │   └── admin.ts                 # 관리자 이메일 설정
│   ├── contexts/
│   │   ├── AuthContext.tsx           # 인증 상태 관리
│   │   ├── CartContext.tsx           # 장바구니 상태 관리
│   │   ├── LanguageContext.tsx       # 한/영 언어 전환
│   │   ├── ThemeContext.tsx          # 다크모드/컬러테마
│   │   └── ToastContext.tsx          # 알림 토스트 메시지
│   ├── hooks/
│   │   ├── useAOS.ts                # Animate on Scroll 훅
│   │   └── useCountUp.ts            # 숫자 카운트업 애니메이션
│   ├── layouts/
│   │   └── PublicLayout.tsx          # 라우트 정의 (Lazy loading)
│   ├── pages/
│   │   ├── Home.tsx                  # 메인 홈페이지
│   │   ├── Login.tsx                 # 로그인
│   │   ├── Register.tsx              # 회원가입
│   │   ├── ForgotPassword.tsx        # 비밀번호 찾기
│   │   ├── MyPage.tsx                # 마이페이지
│   │   ├── Cart.tsx                  # 장바구니
│   │   ├── Checkout.tsx              # 결제
│   │   ├── OrderConfirmation.tsx     # 주문 확인
│   │   ├── OrderHistory.tsx          # 주문 내역
│   │   ├── NotFound.tsx              # 404 페이지
│   │   ├── research-design/
│   │   │   └── ResearchDesign.tsx    # 연구설계 (6개 섹션)
│   │   ├── sampling/
│   │   │   └── Sampling.tsx          # 표본추출 (6개 섹션)
│   │   ├── measurement/
│   │   │   └── Measurement.tsx       # 측정과 척도 (6개 섹션)
│   │   ├── survey/
│   │   │   └── Survey.tsx            # 설문조사법 (6개 섹션)
│   │   ├── experiment/
│   │   │   └── Experiment.tsx        # 실험연구 (6개 섹션)
│   │   ├── qualitative/
│   │   │   └── Qualitative.tsx       # 질적연구 (6개 섹션)
│   │   ├── statistics/
│   │   │   └── Statistics.tsx        # 통계분석 (6개 섹션)
│   │   └── ethics/
│   │       └── Ethics.tsx            # 연구윤리 (6개 섹션)
│   ├── styles/
│   │   ├── base.css                  # 기본 변수/레이아웃 (#1E40AF)
│   │   ├── navbar.css                # 내비바 스타일
│   │   ├── hero.css                  # 히어로 섹션
│   │   ├── home.css                  # 홈페이지 전용
│   │   ├── guide-pages.css           # 학습 가이드 페이지 (사이드바+콘텐츠)
│   │   ├── code-blocks.css           # 코드 블록 스타일
│   │   ├── footer.css                # 푸터 스타일
│   │   ├── community.css             # 커뮤니티 스타일
│   │   ├── auth.css                  # 인증 페이지 스타일
│   │   ├── toast.css                 # 토스트 알림
│   │   ├── dark-mode.css             # 다크모드 오버라이드
│   │   ├── animations.css            # 애니메이션 키프레임
│   │   └── responsive.css            # 반응형 미디어 쿼리
│   ├── types/
│   │   └── index.ts                  # TypeScript 타입 정의
│   ├── utils/
│   │   ├── supabase.ts               # Supabase 클라이언트
│   │   ├── auth.ts                   # 인증 유틸리티
│   │   ├── portone.ts                # PortOne 결제
│   │   ├── storage.ts                # 스토리지 유틸리티
│   │   ├── productStorage.ts         # 상품 스토리지
│   │   ├── commentStorage.ts         # 댓글 스토리지
│   │   ├── searchStorage.ts          # 검색 스토리지
│   │   └── translations.ts           # 한/영 번역 데이터
│   ├── App.tsx                       # 앱 루트 (Providers 래핑)
│   ├── main.tsx                      # 엔트리 포인트
│   ├── index.css                     # 스타일 import 허브
│   └── vite-env.d.ts                 # Vite 타입 선언
├── index.html                        # HTML 엔트리 + OG 메타태그
├── package.json                      # 의존성 (dreamitbiz-research)
├── tsconfig.json                     # TypeScript 설정
├── vite.config.ts                    # Vite 설정 (port 5182)
├── .env.example                      # 환경변수 템플릿
└── .gitignore                        # Git 무시 파일
```

## 총 파일 수

| 카테고리 | 파일 수 |
|----------|---------|
| 설정 파일 | 7 (package.json, vite.config, tsconfig, index.html, .env.example, .gitignore, CNAME) |
| 컴포넌트 | 13 (layout 2개 + 공유 11개) |
| Context | 5 |
| 페이지 | 18 (일반 10개 + 학습경로 8개) |
| 스타일 | 13 CSS |
| 유틸리티 | 8 (7개 + translations) |
| 훅 | 2 |
| 설정 | 2 (site.ts, admin.ts) |
| 배포 | 4 (deploy.yml, CNAME, robots.txt, sitemap.xml) |
| **합계** | **~72개 파일** |
